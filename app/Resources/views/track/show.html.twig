{% extends 'base.html.twig' %}

{% block body %}
    <h1>Track</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ track.id }}</td>
            </tr>
            <tr>
                <th>Make</th>
                <td>{{ track.make }}</td>
            </tr>
            <tr>
                <th>Model</th>
                <td>{{ track.model }}</td>
            </tr>
            <tr>
                <th>Miles</th>
                <td>{{ track.miles }}</td>
            </tr>
            <tr>
                <th>Bodytype</th>
                <td>{{ track.bodyType }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('track_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('track_edit', { 'id': track.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
